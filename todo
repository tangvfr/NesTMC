O multi folder for rom repo
O make emulate mcnes (tmcnes in jframe)

#make manager element divisé & grid(avec index pour l'objet)
#make os gui 

-make message(config) & value configurable
-save nes after reload
-add destruct nes - is linked (in controller)
/reloadrom

#make os gui