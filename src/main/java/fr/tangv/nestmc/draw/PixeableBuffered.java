package fr.tangv.nestmc.draw;

public interface PixeableBuffered {

	/**
	 * Retourne les couleur qui compose la map
	 * @return le buffer des données des couleurs de la map
	 */
	public byte[] getBuffer();
	
}
